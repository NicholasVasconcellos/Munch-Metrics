# Progress Log
# Agents: append your session notes below. Include task number, status, what changed, and anything the next agent needs to know.
# Format:
#   === Task NNN | Status: complete/incomplete | [timestamp] ===
#   CHANGES: ...
#   ISSUES: ...
#   NEXT STEPS: ...

=== Task 001 | Status: complete | 2026-03-28 ===
CHANGES: Full project scaffolding. Next.js 15 (App Router), TypeScript strict mode, Tailwind CSS v4 (CSS-first config via @import and @theme inline), shadcn/ui components manually written (button, dialog, popover, dropdown-menu, toggle, separator), ThemeProvider (next-themes), ThemeToggle (light/dark/system), AccentPicker (9 presets: blue/green/teal/orange/purple/rose/yellow/indigo/pink via data-accent attribute on :root, persisted in localStorage), Header/Footer/MobileNav layout shell, root layout with Geist fonts, placeholder page.tsx, drizzle.config.ts, .env.local.example, components.json (shadcn config).
NOTES: pnpm was not installed in the environment - installed globally via npm. Build scripts for esbuild/sharp/unrs-resolver required adding pnpm.onlyBuiltDependencies to package.json. tw-animate-css used instead of tailwindcss-animate (Tailwind v4 compatible). Accent colors implemented as :root[data-accent="X"] CSS selectors, not JS injection.

=== Task 002 | Status: complete | 2026-03-28 ===
CHANGES: Drizzle schema (8 tables: foods, nutrients, price_data, bls_food_crosswalk, food_images, food_tags, user_preferences, saved_views) with 5 pgEnums. Drizzle client (index.ts) with node-postgres pool + SSL. Migration runner (migrate.ts). TypeScript types: food.ts (inferred + FoodComputed interface), filters.ts, table.ts. drizzle-kit generate produced 0000_lonely_namora.sql. Custom migration 0001_food_computed_view.sql: pg_trgm extension, GIN indexes, materialized view with pivoted nutrients + COALESCE price + protein_per_dollar metric, view indexes.
NOTES: tests/011_landing.sh was already in the repo as untracked before this task and fails on landing components that don't exist yet — not a regression from task 002. drizzle-kit generate does not need a live DB; passing a dummy DATABASE_URL is sufficient.

=== Task 003 | Status: complete | 2026-03-28 ===
CHANGES: scripts/seed-usda.ts — fetches Foundation and SR Legacy foods from USDA FoodData Central API (/v1/foods/list, 200 items/page, 4 s delay between pages). Page-level batch upsert into foods (onConflictDoUpdate on fdc_id), delete-then-reinsert nutrients per food for idempotency. classifyNutrient() maps nutrient names to the 5 enum categories (macronutrient/vitamin/mineral/fatty_acid/amino_acid/other). Refreshes food_computed materialized view after all inserts. Requires DATABASE_URL and FDC_API_KEY env vars. tests/003_seed_usda.sh: 33 file-content + tsc checks, all pass.
NOTES: 011_landing.sh regression failure is pre-existing (task 002 notes confirm this) — landing components not yet built. run_all.sh stops at 011 but 001/002/003 all pass. No tsx package in devDependencies; user should run via `pnpm dlx tsx scripts/seed-usda.ts` or add tsx to devDependencies before running the script.

=== Task 004 | Status: complete | 2026-03-28 ===
CHANGES: scripts/bls-crosswalk.ts — 67 BLS series entries (APU0000XXXXXX format) covering beef, pork, poultry, seafood, eggs, dairy, grains, beverages, vegetables, and fruits. Unit conversion constants (PER_LB=0.22046, PER_DOZEN=0.16667, PER_HALF_GAL=0.05285, etc.) with documented math. scripts/seed-prices.ts — reads crosswalk, looks up foods via ilike, batches BLS API v2 POST requests (≤50 series/batch), picks most recent non-annual (non-M13) period, converts to price_per_100g, upserts bls_food_crosswalk (ON CONFLICT on bls_series_id), delete-then-insert price_data for BLS rows (source='bls'), deletes and reinserts 25 USDA category fallback estimates (source='usda_estimate', food_group only, no food_id), refreshes food_computed materialized view. tests/004_seed_prices.sh — 60 assertions, all passing.
NOTES: 011_landing.sh is a pre-existing regression (landing components not yet built). priceSourceEnum values are 'bls'/'usda_estimate'/'manual' — NOT 'bls_average_price'/'usda_category_estimate' as task description suggests; seed script uses the correct enum values. No tsx in devDependencies; run via `pnpm dlx tsx scripts/seed-prices.ts`.

=== Task 005 | Status: complete | 2026-03-28 ===
CHANGES: scripts/seed-tags.ts — loads all foods and nutrients in two bulk queries, builds per-food nutrient profile (protein/carbs/fat/sodium via keyword matching on nutrient names), derives dietary tags (vegan, vegetarian, pescatarian, keto, high_protein, low_sodium, gluten_free, dairy_free), allergen tags (contains_dairy, contains_gluten, contains_nuts, contains_soy, contains_eggs, contains_fish, contains_shellfish), and processing_level tags (whole_food, minimally_processed, processed) via food_group keyword sets. Clear-and-reinsert strategy for idempotency (deletes dietary/allergen/processing_level tags then batch inserts 500 at a time). Logs tag distribution summary grouped by tagType+tagValue. tests/005_seed_tags.sh — 56 assertions, all passing.
NOTES: 011_landing.sh is a pre-existing regression. No tsx in devDependencies; run via `pnpm dlx tsx scripts/seed-tags.ts`. Shellfish detection uses both group-level ('shellfish' in food_group) and name-level keywords (shrimp, crab, lobster, etc.) within finfish groups. Soy detection requires both legume group AND soy keyword in name/description to avoid false positives.

=== Task 006 | Status: complete | 2026-03-28 ===
CHANGES: src/lib/constants.ts (NUTRIENTS array with 9 nutrients, DIETARY_PRESETS with 8 presets, SORT_COLUMN_MAP, NUTRIENT_COLUMN_MAP). src/lib/queries/search-foods.ts (searchFoods server action: pg_trgm fuzzy search, dynamic WHERE for all filter types via sql template tag with sql.raw whitelist for column names, GROUP BY support for foodGroup/dataSource/processingLevel, paginated). src/lib/queries/get-food-detail.ts (getFoodDetail server action: joins foods + nutrients by category + price_data + food_images + food_tags). src/lib/queries/get-filter-options.ts (getFilterOptions server action: distinct foodGroups/dietaryTags/allergenTags/processingLevels + min/max nutrient ranges). src/app/api/images/[foodId]/route.ts (Unsplash image proxy: cache-first via food_images, onConflictDoUpdate on miss, placeholder on rate-limit/error, AbortSignal timeout). tests/006_queries.sh: 78 assertions, all passing.
NOTES: db.execute() in Drizzle 0.41 requires T extends Record<string,unknown> — removed generic type params and used runtime casting instead. 'pescatarian' is not in DietaryTag union type (filters.ts) so omitted from DIETARY_PRESETS dietary array; the tag exists in food_tags via seed-tags but cannot be used as a filter key without updating the type. 011_landing.sh is pre-existing regression (landing components not yet built).
